name: ronin-php
summary: A Ruby library for Ronin that provides support for PHP related security tasks.
description:
  Ronin PHP is a Ruby library for Ronin that provides support for PHP related security tasks.

license: GPL-2
authors: Postmodern
email: postmodern.mod3@gmail.com
homepage: http://github.com/ronin-ruby/ronin-php
has_yard: true

dependencies:
  uri-query_params: ~> 0.5.0
  data_paths: ~> 0.2.1
  ffi-msgpack: ~> 0.1.4
  cssmin: ~> 1.0.2
  jsmin: ~> 1.0.1
  # Ronin dependencies:
  # ronin-support: ~> 0.1.0
  # ronin: ~> 1.0.0
  # ronin-gen: ~> 0.3.0
  # ronin-scanners: ~> 0.2.0
  # ronin-web: ~> 0.3.0
  # ronin-exploits: ~> 0.4.0

development_dependencies:
  bundler: ~> 1.0.0
  yard: ~> 0.6.1
